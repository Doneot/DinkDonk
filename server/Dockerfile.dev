FROM node:22-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package*.json ./
RUN npm ci

# Copy source (will be overridden by bind mount in dev, but useful for non-mounted runs)
COPY . .

EXPOSE 3000