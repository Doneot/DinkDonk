global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: dinkdonk-backend
    metrics_path: /metrics
    authorization:
      credentials_file: /run/secrets/metrics_token
    static_configs:
      - targets: ["backend:3000"]
